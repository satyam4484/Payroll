import React from 'react'
import spinner from '@/assets/icons/Spinner.svg';

const Spinner: React.FC = () => {
    return (

        <img src={spinner} alt="spinner" className="h-10" />

    )
}

export default Spinner